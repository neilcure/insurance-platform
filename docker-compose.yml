services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    network_mode: host
    env_file:
      - .env
    volumes:
      - uploads:/app/.uploads

volumes:
  uploads:
